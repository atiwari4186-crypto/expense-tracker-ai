import { ExportHubClient } from "@/components/export-hub/ExportHubClient";

export const metadata = { title: "Export Hub — SpendWise" };

export default function ExportPage() {
  return <ExportHubClient />;
}
